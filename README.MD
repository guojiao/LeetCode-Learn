#LeetCode Learn
@(leetcode)[算法|面试]

[TOC]

###1  [Two Sum][1]

>从一个只包含`整形（Integer）`的数组中，返回相加之和等于指定数值的2个数字在数组中的索引。

>Given an array of **integers**, return indices of the two numbers such that they add up to a specific target.


**Example：**
```
Given nums = [2, 7, 11, 15], target = 9,

Because nums[0] + nums[1] = 2 + 7 = 9,
return [0, 1].
```


###2  [Add Two Numbers][2]

>有2个用`链表形式`表示的`非负整数`，链表中的每个元素的val都是数字的某个位置上的数字，`链表中各元素的链接顺序和数字的显示顺序是相反的`。现在需要将2个数字相加，并将和以同样的链表方式返回。

>You are given two **linked lists** representing two **non-negative** numbers. The digits are stored in **reverse order** and each of their nodes contain a single digit. Add the two numbers and return it as a linked list.

**Example：**
```
Input: (2 -> 4 -> 3) + (5 -> 6 -> 4)
Output: 7 -> 0 -> 8
```


###3  [Longest Substring Without Repeating Characters][3]
>从给定的字符串中获取`最长的`、`没有重复字符`的子串的长度

>Given a string, find the length of the **longest** substring **without repeating characters**.

**Examples:**
```
1 Given "abcabcbb", the answer is "abc", which the length is 3.
2 Given "bbbbb", the answer is "b", with the length of 1.
3 Given "pwwkew", the answer is "wke", with the length of 3. Note that the answer must be a substring, "pwke" is a subsequence and not a substring.
```




[1]: https://github.com/guojiao/LeetCode-Learn/tree/master/0001_Two_Sum
[2]: https://github.com/guojiao/LeetCode-Learn/tree/master/0002_Add_Two_Numbers
[3]: https://github.com/guojiao/LeetCode-Learn/tree/master/0003_Longest_Substring_Without_Repeating_Characters